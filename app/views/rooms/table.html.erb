<div>
  <ul>
    <% @rooms.each do |room| %>
      <li><%=link_to "ROOM#{room.id}", "/rooms/#{room.id}/" %><p><%= "/rooms/room_#{room.id}"%></p></li>
    <% end %>
  </ul>
  <li><%=link_to "NEW ROOM", "/rooms/new" %></li>
</div>